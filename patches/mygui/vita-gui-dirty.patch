--- a/MyGUIEngine/include/MyGUI_ILayer.h	2026-08-18 11:42:15.432749561 -0600
+++ b/MyGUIEngine/include/MyGUI_ILayer.h	2026-08-18 11:42:15.410749561 -0600
@@ -60,6 +60,14 @@
 		// render layer
 		virtual void renderToTarget(IRenderTarget* _target, bool _update) = 0;
 
+		// Vita: dirty query for render-skip. Default true = always rebuild
+		// (old behaviour) for layer types that don't track dirtiness;
+		// SharedLayer/OverlappedLayer already implement matching overrides.
+		virtual bool isOutOfDate() const
+		{
+			return true;
+		}
+
 		virtual void resizeView(const IntSize& _viewSize) = 0;
 
 	protected:
--- a/MyGUIEngine/include/MyGUI_ISubWidget.h	2026-08-18 11:42:15.432749561 -0600
+++ b/MyGUIEngine/include/MyGUI_ISubWidget.h	2026-08-18 11:42:15.410749561 -0600
@@ -15,6 +15,16 @@
 #include "MyGUI_IStateInfo.h"
 #include "MyGUI_IObject.h"
 
+#ifdef __vita__
+// Vita GUI-dirty attribution probe: every sub-widget dirty site reports
+// itself. Weak hook; absent = no-op.
+namespace MyGUI { class ISubWidget; }
+extern "C" void vitaGuiSubDirty(MyGUI::ISubWidget* sub) __attribute__((weak));
+#define VITA_GUI_DIRTY_NOTE(sub) do { if (vitaGuiSubDirty) vitaGuiSubDirty(sub); } while (0)
+#else
+#define VITA_GUI_DIRTY_NOTE(sub) do { } while (0)
+#endif
+
 namespace MyGUI
 {
 
--- a/MyGUIEngine/src/MyGUI_EditText.cpp	2026-08-18 11:42:15.432749561 -0600
+++ b/MyGUIEngine/src/MyGUI_EditText.cpp	2026-08-18 11:42:15.410749561 -0600
@@ -33,13 +33,13 @@
 		mVisible = _visible;
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	void EditText::_correctView()
 	{
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	void EditText::_setAlign(const IntSize& _oldsize)
@@ -114,7 +114,7 @@
 
 				// обновить перед выходом
 				if (nullptr != mNode)
-					mNode->outOfDate(mRenderItem);
+					{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 				return;
 			}
 		}
@@ -130,7 +130,7 @@
 		mIsMargin = margin;
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	void EditText::setCaption(const UString& _value)
@@ -142,7 +142,7 @@
 		checkVertexSize();
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	void EditText::checkVertexSize()
@@ -185,7 +185,7 @@
 		mInverseColourNative = mCurrentColourNative ^ 0x00FFFFFF;
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	const Colour& EditText::getTextColour() const
@@ -204,7 +204,7 @@
 		mInverseColourNative = mCurrentColourNative ^ 0x00FFFFFF;
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	void EditText::setFontName(std::string_view _value)
@@ -239,7 +239,7 @@
 		}
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	std::string_view EditText::getFontName() const
@@ -257,7 +257,7 @@
 		mTextOutDate = true;
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	int EditText::getFontHeight() const
@@ -304,7 +304,7 @@
 		mEndSelect = _end;
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	bool EditText::getSelectBackground() const
@@ -319,7 +319,7 @@
 		mBackgroundNormal = _normal;
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	bool EditText::isVisibleCursor() const
@@ -334,7 +334,7 @@
 		mVisibleCursor = _value;
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	size_t EditText::getCursorPosition() const
@@ -349,7 +349,7 @@
 		mCursorPosition = _index;
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	void EditText::setTextAlign(Align _value)
@@ -357,7 +357,7 @@
 		mTextAlign = _value;
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	Align EditText::getTextAlign() const
@@ -396,7 +396,7 @@
 		mViewOffset = _point;
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	IntPoint EditText::getViewOffset() const
@@ -443,7 +443,7 @@
 		mShiftText = _value;
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	void EditText::setWordWrap(bool _value)
@@ -452,7 +452,7 @@
 		mTextOutDate = true;
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	void EditText::updateRawData() const
@@ -604,7 +604,7 @@
 		mInvertSelect = _value;
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	bool EditText::getInvertSelected() const
@@ -625,7 +625,7 @@
 		checkVertexSize();
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	void EditText::setShadowColour(const Colour& _value)
@@ -636,7 +636,7 @@
 		mShadowColourNative = (mShadowColourNative & 0x00FFFFFF) | getMixedNativeAlpha(mShadowColour.alpha);
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	const Colour& EditText::getShadowColour() const
--- a/MyGUIEngine/src/MyGUI_PolygonalSkin.cpp	2026-08-18 11:42:15.432749561 -0600
+++ b/MyGUIEngine/src/MyGUI_PolygonalSkin.cpp	2026-08-18 11:42:15.419549560 -0600
@@ -93,7 +93,7 @@
 		mGeometryOutdated = true;
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	void PolygonalSkin::setAlpha(float _alpha)
@@ -102,7 +102,7 @@
 		mCurrentColour = (mCurrentColour & 0x00FFFFFF) | (alpha & 0xFF000000);
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	void PolygonalSkin::_correctView()
@@ -110,7 +110,7 @@
 		mGeometryOutdated = true;
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	void PolygonalSkin::_setAlign(const IntSize& _oldsize)
@@ -176,7 +176,7 @@
 
 				// обновить перед выходом
 				if (nullptr != mNode)
-					mNode->outOfDate(mRenderItem);
+					{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 				return;
 			}
 		}
@@ -192,7 +192,7 @@
 		mIsMargin = margin;
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	void PolygonalSkin::createDrawItem(ITexture* _texture, ILayerNode* _node)
@@ -253,7 +253,7 @@
 		mCurrentColour = (colour & 0x00FFFFFF) | (mCurrentColour & 0xFF000000);
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	void PolygonalSkin::setStateData(IStateInfo* _data)
@@ -268,7 +268,7 @@
 		mGeometryOutdated = true;
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	void PolygonalSkin::_rebuildGeometry()
--- a/MyGUIEngine/src/MyGUI_RenderItem.cpp	2026-08-18 11:42:15.432749561 -0600
+++ b/MyGUIEngine/src/MyGUI_RenderItem.cpp	2026-08-18 11:42:15.410749561 -0600
@@ -191,7 +191,9 @@
 
 	bool RenderItem::isOutOfDate() const
 	{
-		return mOutOfDate;
+		// Vita: textureless items render nothing and renderToTarget
+		// early-outs before clearing the flag; report them clean.
+		return mOutOfDate && (mTexture != nullptr);
 	}
 
 	size_t RenderItem::getNeedVertexCount() const
--- a/MyGUIEngine/src/MyGUI_RotatingSkin.cpp	2026-08-18 11:42:15.432749561 -0600
+++ b/MyGUIEngine/src/MyGUI_RotatingSkin.cpp	2026-08-18 11:42:15.415149562 -0600
@@ -25,7 +25,7 @@
 		mGeometryOutdated = true;
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	void RotatingSkin::setCenter(const IntPoint& _center)
@@ -34,7 +34,7 @@
 		mGeometryOutdated = true;
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	IntPoint RotatingSkin::getCenter(bool _local) const
@@ -51,7 +51,7 @@
 		mGeometryOutdated = true;
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	void RotatingSkin::setAlpha(float _alpha)
@@ -60,7 +60,7 @@
 		mCurrentColour = (mCurrentColour & 0x00FFFFFF) | (alpha & 0xFF000000);
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	void RotatingSkin::_correctView()
@@ -68,7 +68,7 @@
 		mGeometryOutdated = true;
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	void RotatingSkin::_setAlign(const IntSize& _oldsize)
@@ -119,7 +119,7 @@
 		mGeometryOutdated = true;
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	void RotatingSkin::createDrawItem(ITexture* _texture, ILayerNode* _node)
@@ -189,7 +189,7 @@
 		mCurrentColour = (colour & 0x00FFFFFF) | (mCurrentColour & 0xFF000000);
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	void RotatingSkin::setStateData(IStateInfo* _data)
@@ -209,7 +209,7 @@
 		mGeometryOutdated = true;
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	inline float len(float x, float y)
--- a/MyGUIEngine/src/MyGUI_SharedLayer.cpp	2026-08-18 11:42:15.432749561 -0600
+++ b/MyGUIEngine/src/MyGUI_SharedLayer.cpp	2026-08-18 11:42:15.410749561 -0600
@@ -167,7 +167,8 @@
 
 	bool SharedLayer::isOutOfDate() const
 	{
-		if (mChildItem->isOutOfDate())
+		// Vita: mChildItem is created lazily on first attach.
+		if (mChildItem != nullptr && mChildItem->isOutOfDate())
 			return true;
 
 		return mOutOfDate;
--- a/MyGUIEngine/src/MyGUI_SubSkin.cpp	2026-08-18 11:42:15.432749561 -0600
+++ b/MyGUIEngine/src/MyGUI_SubSkin.cpp	2026-08-18 11:42:15.415149562 -0600
@@ -29,7 +29,7 @@
 		mVisible = _visible;
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	void SubSkin::setAlpha(float _alpha)
@@ -38,13 +38,13 @@
 		mCurrentColour = (mCurrentColour & 0x00FFFFFF) | (alpha & 0xFF000000);
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	void SubSkin::_correctView()
 	{
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	void SubSkin::_setAlign(const IntSize& _oldsize)
@@ -109,7 +109,7 @@
 
 				// обновить перед выходом
 				if (nullptr != mNode)
-					mNode->outOfDate(mRenderItem);
+					{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 				return;
 			}
 		}
@@ -150,7 +150,7 @@
 		mIsMargin = margin;
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	void SubSkin::createDrawItem(ITexture* _texture, ILayerNode* _node)
@@ -202,7 +202,7 @@
 		}
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	void SubSkin::doRender()
@@ -250,7 +250,7 @@
 		mCurrentColour = (colour & 0x00FFFFFF) | (mCurrentColour & 0xFF000000);
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	void SubSkin::setStateData(IStateInfo* _data)
--- a/MyGUIEngine/src/MyGUI_TileRect.cpp	2026-08-18 11:42:15.432749561 -0600
+++ b/MyGUIEngine/src/MyGUI_TileRect.cpp	2026-08-18 11:42:15.415149562 -0600
@@ -32,7 +32,7 @@
 		mVisible = _visible;
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	void TileRect::setAlpha(float _alpha)
@@ -41,13 +41,13 @@
 		mCurrentColour = (mCurrentColour & 0x00FFFFFF) | (alpha & 0xFF000000);
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	void TileRect::_correctView()
 	{
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	void TileRect::_setAlign(const IntSize& _oldsize)
@@ -143,7 +143,7 @@
 
 				// обновить перед выходом
 				if (nullptr != mNode)
-					mNode->outOfDate(mRenderItem);
+					{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 				return;
 			}
 		}
@@ -152,14 +152,14 @@
 		mIsMargin = margin;
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	void TileRect::_setUVSet(const FloatRect& _rect)
 	{
 		mCurrentTexture = _rect;
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 	void TileRect::doRender()
@@ -353,7 +353,7 @@
 		mCurrentColour = (colour & 0x00FFFFFF) | (mCurrentColour & 0xFF000000);
 
 		if (nullptr != mNode)
-			mNode->outOfDate(mRenderItem);
+			{ VITA_GUI_DIRTY_NOTE(this); mNode->outOfDate(mRenderItem); }
 	}
 
 } // namespace MyGUI
